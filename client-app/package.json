{
  "name": "noip-client-app",
  "version": "1.0.0",
  "description": "Periodicaly makes call to a server, to make server to check if client ip has changed.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "bozjator",
  "license": "ISC",
  "dependencies": {
    "request": "2.88.0",
    "request-promise": "4.2.2",
    "winston": "3.1.0"
  }
}
